# Renderer Assets

Place local UI assets here when the renderer needs bundled artwork. Runtime game covers should stay in the user's own
cover folders and are loaded through the safe `retro-media://` protocol.
